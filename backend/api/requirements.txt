fastapi==0.111.*
uvicorn[standard]==0.29.*
sqlalchemy[asyncio]==2.0.*
asyncpg==0.29.*
psycopg[binary]==3.*
alembic==1.13.*
redis[hiredis]==5.*
pydantic==2.*
pydantic-settings==2.*
# Floors set at patched releases for known advisories:
#   python-jose  >=3.4.0  (algorithm-confusion / DoS CVE-2024-33663/33664)
#   python-multipart >=0.0.7 (ReDoS on malformed multipart boundaries, CVE-2024-24762)
#   Pillow       >=10.3.0 (image-parsing CVEs)
python-jose[cryptography]>=3.4.0,<4
passlib[bcrypt]==1.7.*
bcrypt<4.0.0
python-multipart>=0.0.7,<0.1
Pillow>=10.3.0,<11
aiofiles==23.*
httpx==0.27.*
jinja2>=3.1.4,<4
google-auth==2.*
# google-auth's verify_firebase_token uses google.auth.transport.requests,
# which requires the `requests` package (it's an optional extra, not a
# hard dependency, so we list it explicitly).
requests==2.*
python-magic==0.4.*
pytest==8.*
pytest-asyncio==0.23.*
